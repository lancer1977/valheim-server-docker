# Workflows

- `.github/workflows/validate.yml` runs the fast local validation script.
- `.github/workflows/docker-build.yml` builds, tests, and publishes the Docker image.
- `.github/workflows/codeql-analysis.yml` runs CodeQL security analysis.
